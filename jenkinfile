pipeline {
    agent { label 'dev' }  // Run on slave machine

    environment {
        DOCKER_IMAGE = "node_app"
        DOCKER_TAG = "latest"
    }

    stages {
        stage('Clone Repository') {
            steps {
                git 'https://github.com/Arun-2095/e-commerce/'  // Clones only changes
            }
        }

        stage('Build & Deploy Docker Container') {
            steps {
                script {
                    // Stop and remove existing container
                    sh "sudo docker stop node_app || true"
                    sh "sudo docker rm node_app || true"

                    // Build Docker image (npm install happens inside)
                    sh "sudo docker build -t $DOCKER_IMAGE:$DOCKER_TAG ."

                    // Run new container
                    sh "sudo docker run -d --name node_app -p 2222:2222 $DOCKER_IMAGE:$DOCKER_TAG"
                }
            }
        }
    }

    post {
        success {
            echo "Deployment successful!"
        }
        failure {
            echo "Build failed!"
        }
    }
}
